dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
services:
  - xvfb
variables:
    QT_QPA_PLATFORM: "offscreen"
install:
  - pip install pipenv
  - pipenv install --dev
script:
  - pytest -s
